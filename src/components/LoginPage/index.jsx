import React from 'react';
import UsernameForm from '../UsernameForm';

const LoginPage = ({ handleUserSubmitted }) => <UsernameForm handleUserSubmitted={handleUserSubmitted} />;

export default LoginPage;

